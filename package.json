{
    "name": "aws-lambda-layer-cache",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "build": "cd src/aws-lambda-layer-cache && npm install --production && chmod +x *.js && cd ../extensions && chmod +x aws-lambda-layer-cache && cd ../../",
        "deploy": "npm run build && sls deploy --stage"
    }
}
