{
    "name": "aws-lambda-layer-cache",
    "version": "1.1.0",
    "license": "MIT",
    "scripts": {
        "build": "cd src/aws-lambda-layer-cache && npm install --omit dev && chmod +x *.js && cd ../extensions && chmod +x aws-lambda-layer-cache && cd ../../",
        "deploy": "npm run build && serverless deploy"
    },
    "devDependencies": {
        "serverless": "^3.32.2"
    }
}
