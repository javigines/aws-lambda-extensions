org: webel
app: aws-lambda-layer-cache
name: aws-lambda-layer-cache
component: aws-lambda-layer
region: eu-west-1

inputs:
  name: ${app}-${stage}
  region: eu-west-1
  src: ./src
  runtimes:
    - nodejs14.x
